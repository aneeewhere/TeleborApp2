<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teleborapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Телебор"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeActivity" />
        <activity android:name=".EditAbonentActivity" />
        <activity android:name=".ChangeTariffActivity" />
        <activity android:name=".CreateRequestActivity" />
        <activity android:name=".SupportActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".AdminEditUserActivity" />

    </application>

</manifest>